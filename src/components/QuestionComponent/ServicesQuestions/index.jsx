import React from 'react'
import {
    Row, Col, Button,
    Dropdown, DropdownButton,
    ListGroup,
    Accordion, Card, Image
} from 'react-bootstrap'
import Filter from '../../Chat/Filter';
import ProductCard from '../../Chat/ProductCard';
export default function ServicesQuestions({ recommendation }) {
    const openDetail = () => {
        //navigate ile detay sayfasına gideriz
    }
    return (
        <>
            <p>Hizmet için Sorular:</p>
        </>
    )
}
